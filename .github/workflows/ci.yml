name: CI

on:
  push:
  pull_request:

jobs:
  smoke:
    name: Smoke tests
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run test suite
        run: make test
      - name: Build release archive
        run: make package
