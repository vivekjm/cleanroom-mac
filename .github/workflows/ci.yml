name: CI

on:
  push:
  pull_request:

jobs:
  smoke:
    name: Smoke tests
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run smoke tests
        run: ./test/smoke.sh
